# 第三方前端（如 Pelagica）与官方前端的本质区别及数据同步机制分析

为了帮助您理清“继续观看”等数据的同步逻辑，以及彻底理解“第三方前端”和“官方前端”在架构与功能上的本质区别，我们从底层 API 交互和系统架构的视角进行深度拆解。

---

## 一、 澄清误区：“继续观看”到底同不同步？

> [!NOTE]
> **结论：您的播放历史和“继续观看”进度，在所有前端（官方与第三方）中是 100% 同步且完全一致的！**

### 1. 媒体数据的同步（100% 对接并一致）
您在 Chrome 上的 Pelagica 里把一部电影看到第 45 分钟，然后换到手机官方客户端，或者换到 Jellyfin-Vue，打开同一部电影，它**依然会从第 45 分钟开始播放**。
这是因为无论是官方还是第三方前端，它们的底层都调用了 Jellyfin Server 开源且标准的**播放状态汇报 API**：
* 开始播放时调用：`/Sessions/Playing`
* 播放中定时调用（Ticks 进度）：`/Sessions/Playing/Progress`
* 停止播放时调用：`/Sessions/Playing/Stopped`

这些 API 会将您的“已观看时间点”实时写入 **Jellyfin Server 的核心数据库**。因此，任何前端去拉取个人数据时，得到的“继续观看”列表和播放进度都是绝对一致的。

---

### 2. 无法同步的到底是什么？（界面排版与个性化配置）
我们在前文中提到“Jellyfin 无法帮第三方客户端存储”，指的是**“用户界面的排版个性化配置（Layout Settings）”**，而不是播放历史。

例如：
* **布局偏好**：“我希望首页第一行显示‘继续观看’，第二行显示‘最近添加’，并完全隐藏‘推荐音乐’。”
* **自定义模块**：在首页插入一个自定义的媒体栏（Media Bar），展示特定标签（如“高分科幻”）的电影。
* **主题与样式**：用户在 Pelagica 里选择并安装了“毛玻璃暗夜蓝”的主题 CSS。

**为什么这些不能同步？**
因为官方 Jellyfin 服务的后台数据库只预留了官方客户端所需的极简字段（如“是否启用隐藏字幕”）。
Pelagica 拥有一大堆精美的自定义排版、毛玻璃主题、快捷键设置等，Jellyfin Server 的数据库**根本没有对应的表结构**来存储这些第方客户端的个性化参数。
这就是 **Pelagica Go 后端** 的用武之地——它负责帮您把这些“让界面变得无比好看和个性化”的排版配置文件（`config.json` 和自定义 CSS 主题）保存在本地。

---

## 二、 第三方前端 vs 官方前端：四大本质区别

| 对比维度 | 官方前端 (Jellyfin-Web) | 第三方现代前端 (如 Pelagica) |
| :--- | :--- | :--- |
| **项目定位** | **“全能管理门户”**（既是播放器，又是管理员控制台） | **“纯粹的极致播放与观影体验”**（只服务于观众） |
| **架构耦合度** | **强耦合**（绑定在 C# 服务端中，随服务版本一同发布） | **零耦合**（纯粹的 API 消费者，通过 HTTP API 通信） |
| **技术债务** | **极重**（混杂了 Backbone.js, jQuery 以及历史代码） | **极轻**（React 19 + Vite 7，代码极为干净现代） |
| **多源聚合能力** | **零**（只能显示自己 Jellyfin 数据库里的内容） | **无限**（可以同时接入 Emby, 豆瓣, Subtitle 等外部 API） |

### 1. 定位：管理工具 vs 观影神器
* **官方前端**：它不仅是一个播放器，更是一个**“服务器管理后台”**。它必须包含：添加媒体库、配置显卡硬件转码、管理用户权限、配置网络端口、安装服务端插件等成百上千个管理员功能。
* **第三方前端**：Pelagica 这类前端剥离了所有繁琐的系统管理功能，只保留 **“看视频、听音乐、搜索、个性化首页”** 这几个核心观影场景。这让它的体积大大缩小，交互设计可以完全向 Netflix、Apple TV 这种顶级流媒体平台的纯粹观影体验看齐。

### 2. 架构：庞然大物 vs 敏捷利刃
* **官方前端**：由于历史原因，官方 Web 端包含了大量的陈旧代码（Backbone.js、早期 jQuery 和复杂的历史播放逻辑）。由于体量过大，官方团队很难对其进行彻底重构。
* **第三方前端**：完全从零开始，使用现代最流行的 **React 19 + Tailwind CSS** 编写。它不需要考虑怎么去管理服务器，只需要调用服务端暴露出来的开放 API（`@jellyfin/sdk`）。这让它可以做到极速响应，代码结构极其干净，二开门槛极低。

### 3. 多数据源聚合与扩展能力（二开的核心价值）
* **官方前端**：处于严格的“围墙花园”内，绝对不会引入任何非 Jellyfin 官方提供的数据。
* **第三方前端**：具有强大的**“混合集成”**能力。例如，Pelagica 不仅能读取您的 Jellyfin 媒体，还能：
  * 集成 **Streamystats**（外部流媒体统计推荐）。
  * 集成 **Kefintweaks Watchlist**（第三方追剧清单）。
  * **您的二开方向**：这正是您将 `emby2openlist` 与其结合的关键！您可以在 Pelagica 中同时接入您的 Emby 服务器和 Jellyfin 服务器，并直接在一个前端上展示、转换和同步播放列表。这种跨平台、多数据源的融合，在官方前端上是**绝对不可能实现**的。

### 4. 设备的取舍
* **官方前端**：必须做到“全设备通吃”，哪怕是 10 年前买的、内核版本极低的老电视浏览器，它也必须硬着头皮去兼容，因此选型极度受限。
* **第三方前端**：直接放弃低端老旧设备，只面向现代高性能浏览器（PC、手机、平板）。这让它能使用最新的 HLS MSE 解码技术，享受到最丝滑的转场动画和毛玻璃 UI 动效。
